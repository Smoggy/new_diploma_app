<%= form_tag("/send", method: "post") do |f|%>
binding.pry
  <%= f.collection_select :task_id, @tasks, :id, :title %>
  <%= f.collection_select :subject_id, @subjects, :id, :title %>
  <%= f.submit_tag("Send") %>
<% end %>