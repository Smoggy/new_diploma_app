<table>
	<thead>
    <tr>
      <th>Title</th>
      <th>Created at</th>
    </tr>
  </thead>
   <tbody>
		<% @tasks.each do |task| %>
		<tr>
			<td><%= task.title %></td>
			<td><%= task.created_at %></td>
		</tr>
		<% end %>
	</tbody>
</table>
<%= link_to "Add Task", new_task_path %>
