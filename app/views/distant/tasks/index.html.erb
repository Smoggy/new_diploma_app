
<%= render 'distant/layouts/sidebar' %>

	<div class="content-table" >
<table>
	<thead>
    <tr>
      <th>Название</th>
      <th>Создан</th>
      <th></th>
    </tr>
  </thead>
   <tbody>
		<% @tasks.each do |task| %>
		<tr>
			<td><%= task.title %></td>
			<td><%= task.created_at %></td>
		</tr>
		<% end %>
	</tbody>
</table>
</div>
<%= link_to "Добавить задание", new_distant_task_path, class: "button small task_button" %>
